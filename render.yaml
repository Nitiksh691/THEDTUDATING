services:
  - type: web
    name: blind-connection-backend
    env: node
    plan: free
    rootDir: backend-node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18.x
