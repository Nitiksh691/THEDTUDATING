// ─── Centralized Configuration ─────────────────────────────────────────────
// Single source of truth for API URLs. Change here to update everywhere.

export const API_URL =
    process.env.NEXT_PUBLIC_API_URL || "https://thedtudating.onrender.com";
